# Cleanup

Inventory worktrees, processes, test instances, simulators, and caches only in the requested scope. Determine ownership and whether useful changes are committed, merged, backed up, or still active. Preserve untracked work unless it is actually known to be disposable. Ask before irreversible loss outside prior authority. Remove only the confirmed set and verify the resulting state; never kill all processes with a shared name.
