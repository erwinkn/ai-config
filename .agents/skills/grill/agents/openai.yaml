interface:
  display_name: "Grill"
  short_description: "Stress-test a plan one decision at a time"
  default_prompt: "Use $grill to stress-test this idea one decision at a time."
policy:
  allow_implicit_invocation: false
