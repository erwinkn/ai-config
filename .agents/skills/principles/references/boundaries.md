# Keep boundaries and types honest

Parse external inputs into meaningful types. Place validation where assumptions become trustworthy; preserve runtime checks for invariants static types do not establish. Derive shapes from their authoritative source.

## Apply when

design, implement, test, review
