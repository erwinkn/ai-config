#!/bin/bash

set -euo pipefail

if [[ $(uname -s) != "Darwin" ]]; then
    echo "setup-macos: this setup command supports macOS only." >&2
    exit 1
fi

script_dir=$(CDPATH='' cd -- "$(dirname -- "$0")" && pwd)
exec "$script_dir/setup-unix" "$@"
